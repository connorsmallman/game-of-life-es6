{
  "name": "game-of-life-es6",
  "title": "Game of Life ES6",
  "description": "Conway's Game of life - ES6 JavaScript",
  "version": "0.0.1",
  "author": "connorsmallman",
  "main": "./dist/main.js",
  "scripts": {
    "build": "npm run index && npm run bundle",
    "bundle": "browserify -t babelify src/index.js -o dist/bundle.js",
    "index": "rm -rf dist && mkdir dist && cp src/index.html dist/index.html"
  },
  "devDependencies": {
    "babelify": "6.0.2",
    "lodash": "^4.0.0"
  }
}
